import React from "react";
import s from './Music.module.css'

type MusicPropsType = {
    }

export  const Music = (props: MusicPropsType) => {
    return (
        <div>
        Music
        </div>
    )
  };